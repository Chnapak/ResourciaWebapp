/**
 * Example production environment configuration values.
 * Copy to environment.ts and replace placeholders.
 */
export const environment = {
  production: true,
  siteKey: "CHANGEME_TURNSTILE_SITE_KEY"
};
